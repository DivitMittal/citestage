name: citestage
version: 0.1.0
description: APM-managed agent context for CiteStage.
author: Divit Mittal
license: MIT

# Microsoft APM (Agent Package Manager) deploys local primitives from .apm/.
# Keep this project self-contained so the repository can be cloned on its own.
includes: auto

targets:
  - claude

dependencies:
  apm: []
  mcp: []

scripts: {}
